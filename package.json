{
  "name": "vrc-knowledge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-categories.mjs",
    "dev": "vitepress dev docs",
    "prebuild": "node scripts/generate-categories.mjs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}